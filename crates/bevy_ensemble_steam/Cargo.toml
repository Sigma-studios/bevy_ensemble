[package]
name = "bevy_ensemble_steam"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy = "0.18"
bevy-steamworks = { git = "https://github.com/Sigma-studios/bevy_steamworks", branch = "chore/steamworks-rs-0.13.1" }
bevy_ensemble = { path = "../.." }
serde = { version = "1", features = ["derive"] }
steamworks-sys = "0.13.0"

[dev-dependencies]
bevy_immediate = "0.5"
